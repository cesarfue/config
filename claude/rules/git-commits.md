# Git commits

- Subject line: short imperative, lowercase after the type prefix (e.g. `feat(scope): do thing`)
- No `Co-Authored-By` trailers — ever
- No `--no-verify` unless the user explicitly asks
- Prefer several small, logically grouped commits over one large dump
- Never commit on behalf of the user without being asked
